"use client";

import { StudentType } from "@/types";

export const EditStudent = ({ student }: { student: StudentType }) => {
  return (
    <form className="form">
      <h1>Form</h1>
    </form>
  );
};
